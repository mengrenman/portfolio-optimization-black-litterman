name: portfolio-bl
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - numpy>=1.26
  - pandas>=2.1
  - pyyaml>=6.0
  - matplotlib>=3.8
  - jupyter>=1.0
  - ipykernel>=6.0
  - pytest>=8.0
  - ruff>=0.6
  - pip
  - pip:
      - yfinance>=0.2
